import React from 'react';

import {StyledButton, StyledTitle} from './styles';

const LoadButton = () => {
	return (
		<>
			<StyledButton>
				<StyledTitle>Carregar mais tópicos</StyledTitle>
			</StyledButton>
		</>
	);
};

export default LoadButton;
